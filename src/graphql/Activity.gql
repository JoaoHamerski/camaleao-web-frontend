#import "./ActivityFragments.gql"

query activities ($first: Int! $page: Int) {
  activities (first: $first page: $page) {
    data {
      id
      log_name
      description
      subject {
        ...OrderSubject
      }
      causer {
        ...UserCauser
      }
      properties
      created_at
    }
  }
}